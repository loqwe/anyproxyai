# AnyProxyAi

<div align="center">

**通用 AI API 代理路由器 - 支持多格式转换**

[![构建状态](https://github.com/cniu6/anyproxyai/workflows/Build%20All%20Platforms/badge.svg)](https://github.com/cniu6/anyproxyai/actions)
[![许可证: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![Go 版本](https://img.shields.io/badge/Go-1.22+-00ADD8?logo=go)](https://go.dev/)
[![Wails](https://img.shields.io/badge/Wails-v3-blue)](https://wails.io/)

[English](README.md) | [简体中文](README_CN.md)

**💰 此工具价值 100 美金 - 因为花费了等值的 Claude Opus 4.5 辅助开发**

</div>

## 📖 项目简介

AnyProxyAi 是一个通用的 AI API 代理路由器，支持多种 API 格式（OpenAI、Claude、Gemini）之间的自动转换、负载均衡和智能路由。它为所有 AI API 需求提供统一的本地端点。

### 为什么需要 AnyProxyAi？

- **多格式支持**：无缝转换 OpenAI、Claude (Anthropic) 和 Gemini API 格式
- **统一端点**：所有 AI 服务使用单一本地 API 端点
- **负载均衡**：自动在多个 API 端点之间路由
- **使用统计**：实时监控请求数、Token 用量和成功率
- **跨平台**：原生桌面应用支持 Windows、macOS 和 Linux
- **多语言支持**：支持中英文，默认跟随系统语言

## 📸 应用截图

<p align="center">
  <img src="https://github.com/cniu6/anyproxyai/blob/master/img/png1.png?raw=true" alt="主页" width="80%">
  <br/>主页 - 仪表盘
</p>

<p align="center">
  <img src="https://github.com/cniu6/anyproxyai/blob/master/img/png2.png?raw=true" alt="模型列表" width="80%">
  <br/>模型列表 - 路由管理
</p>

<p align="center">
  <img src="https://github.com/cniu6/anyproxyai/blob/master/img/png3.png?raw=true" alt="统计" width="80%">
  <br/>使用统计 - 热力图和图表
</p>

<p align="center">
  <img src="https://github.com/cniu6/anyproxyai/blob/master/img/png4.png?raw=true" alt="添加路由" width="80%">
  <br/>添加路由对话框
</p>

<p align="center">
  <img src="https://github.com/cniu6/anyproxyai/blob/master/img/png5.png?raw=true" alt="Linux 运行" width="80%">
  <br/>Linux 端运行
</p>

<p align="center">
  <img src="https://github.com/cniu6/anyproxyai/blob/master/img/png6.png?raw=true" alt="Claude Code 转 OpenAI" width="80%">
  <br/>Claude Code 接口转换为 OpenAI 格式
</p>

<p align="center">
  <img src="https://github.com/cniu6/anyproxyai/blob/master/img/png7.png?raw=true" alt="OpenAI 格式转换" width="80%">
  <br/>OpenAI 格式转换
</p>

## ✨ 功能特性

### 核心功能

| 功能 | 说明 |
|------|------|
| 🔄 **API 格式转换** | 自动在 OpenAI、Claude 和 Gemini 格式之间转换 |
| 🔀 **智能路由** | 根据模型名称将请求路由到不同后端 |
| 🔁 **代理重定向** | 使用 `proxy_auto` 关键字重定向到任意配置的模型 |
| 📊 **实时统计** | 监控请求数、错误数、Token 使用量 |
| 📈 **历史数据** | 基于 SQLite 的统计数据，热力图可视化 |
| 🎯 **模型排行** | 追踪最常用的模型及其性能 |
| 🌐 **多语言支持** | 中英文支持，默认跟随系统语言，右上角可弹窗切换（持久化保存） |

### 支持的 API 格式

| 格式 | 输入 | 输出 | 流式 | 流式转换 |
|------|------|------|------|----------|
| OpenAI | ✅ | ✅ | ✅ | ✅ |
| Claude (Anthropic) | ✅ | ✅ | ✅ | ✅ |
| Gemini | ✅ | ✅ | ✅ | ✅ |
| Claude Code | ✅ | ✅ | ✅ | ✅ |
| Cursor IDE | ✅ | ✅ | ✅ | ✅ |

### 界面功能

| 功能 | 说明 |
|------|------|
| 🖥️ **跨平台桌面应用** | 支持 Windows、macOS、Linux |
| 🎨 **深色/浅色主题** | 主题切换 |
| 🌐 **语言切换** | 右上角弹窗切换语言（持久化保存） |
| 📋 **系统托盘** | 最小化到系统托盘 |
| 📝 **路由管理** | 添加、编辑、删除、导入/导出路由 |
| 📊 **使用仪表盘** | 热力图、图表和统计数据 |

## 🚀 快速开始

### 下载安装

[📥 下载最新版本](https://github.com/cniu6/anyproxyai/releases/latest)

**可用构建版本：**
- `anyproxyai-windows-amd64.exe` - Windows x64
- `anyproxyai-windows-arm64.exe` - Windows ARM64
- `anyproxyai-linux-amd64` - Linux x64
- `anyproxyai-linux-arm64` - Linux ARM64
- `anyproxyai-darwin-amd64.zip` - macOS Intel
- `anyproxyai-darwin-arm64.zip` - macOS Apple Silicon

#### Windows
1. 下载 `anyproxyai-windows-amd64.exe`
2. 运行可执行文件
3. 如有提示，允许防火墙访问

#### macOS
1. 下载 `anyproxyai-darwin-arm64.zip`（Apple Silicon）或 `anyproxyai-darwin-amd64.zip`（Intel）
2. 解压并将 `anyproxyai.app` 移动到「应用程序」文件夹
3. 首次运行：右键点击 → 打开（绕过 Gatekeeper）

#### Linux
```bash
chmod +x anyproxyai-linux-amd64
./anyproxyai-linux-amd64
```

### 配置步骤

#### 1. 添加 API 路由

点击「添加路由」按钮并配置：

| 字段 | 说明 | 示例 |
|------|------|------|
| **名称** | 友好名称 | `GPT-4 Turbo` |
| **模型** | 模型标识符 | `gpt-4-turbo` |
| **API 地址** | 后端 API URL | `https://api.openai.com` |
| **API 密钥** | 你的 API 密钥 | `sk-xxx...` |
| **分组** | 可选分组 | `OpenAI` |
| **格式** | API 格式类型 | `openai` / `claude` / `gemini` |

#### 2. 配置你的应用程序

在你的应用程序中使用本地代理端点：

**OpenAI 兼容接口：**
```
API Base URL: http://localhost:5642/api
API Key: （使用主页显示的密钥，或禁用验证时可填任意值）
```

**Claude/Anthropic 接口：**
```
API Base URL: http://localhost:5642/api/anthropic
API Key: （使用主页显示的密钥，或禁用验证时可填任意值）
```

**Claude Code 专用接口：**
```
API Base URL: http://localhost:5642/api/claudecode
API Key: （使用主页显示的密钥，或禁用验证时可填任意值）
```

**Gemini 接口：**
```
API Base URL: http://localhost:5642/api/gemini
API Key: （使用主页显示的密钥，或禁用验证时可填任意值）
```

**Cursor IDE 接口：**
```
API Base URL: http://localhost:5642/api/cursor/v1
API Key: （使用主页显示的密钥，或禁用验证时可填任意值）
```

> **关于 Cursor IDE**：Cursor 使用 OpenAI 兼容端点，但工具定义和消息格式采用 Anthropic/Claude 风格。AnyProxyAi 会自动检测并将 Cursor 格式转换为标准 OpenAI 格式，因此你可以在 Cursor 中使用任何 OpenAI 兼容的后端。

> **注意**：主页显示的 API Key 用于身份验证。如需禁用验证，请在 `config.json` 中将 `local_api_key` 设置为空字符串。

#### 3. 使用代理重定向（可选）

启用重定向并将 `proxy_auto` 设置为模型名称，自动路由到配置的目标模型。

## 📖 系统架构

```
┌─────────────────┐     ┌─────────────────────────────────────────────────────┐
│  你的应用       │────▶│                 AnyProxyAi                          │
│  (任意 SDK)     │     │  localhost:5642                                     │
└─────────────────┘     │                                                     │
                        │  ┌─────────────────────────────────────────────┐   │
                        │  │              API 路由器                      │   │
                        │  │  /api/v1/*        → OpenAI 格式             │   │
                        │  │  /api/cursor/*    → Cursor IDE 格式         │   │
                        │  │  /api/anthropic/* → Claude 格式             │   │
                        │  │  /api/claudecode/*→ Claude Code 格式        │   │
                        │  │  /api/gemini/*    → Gemini 格式             │   │
                        │  └─────────────────────────────────────────────┘   │
                        │                        │                            │
                        │  ┌─────────────────────▼─────────────────────────┐ │
                        │  │           格式适配器                           │ │
                        │  │  ┌──────────┐ ┌──────────┐ ┌──────────┐       │ │
                        │  │  │ OpenAI   │ │ Claude   │ │ Gemini   │       │ │
                        │  │  │ 适配器   │ │ 适配器   │ │ 适配器   │       │ │
                        │  │  └──────────┘ └──────────┘ └──────────┘       │ │
                        │  │  ┌──────────┐                                 │ │
                        │  │  │ Cursor   │ (自动检测并转换)                │ │
                        │  │  │ 适配器   │                                 │ │
                        │  │  └──────────┘                                 │ │
                        │  └───────────────────────────────────────────────┘ │
                        │                        │                            │
                        │  ┌─────────────────────▼─────────────────────────┐ │
                        │  │           后端路由（云端）                      │ │
                        │  │  ┌──────────┐ ┌──────────┐ ┌──────────┐       │ │
                        │  │  │ OpenAI   │ │ Claude   │ │ Gemini   │       │ │
                        │  │  │ 云端     │ │ 云端     │ │ 云端     │       │ │
                        │  │  └──────────┘ └──────────┘ └──────────┘       │ │
                        │  └───────────────────────────────────────────────┘ │
                        └─────────────────────────────────────────────────────┘
```

### 请求流程

1. **接收请求**：本地代理接收 API 请求
2. **路由匹配**：根据模型名称查找匹配的路由
3. **格式检测**：从请求路径检测源格式
4. **适配器选择**：根据路由格式选择适当的适配器
5. **请求转换**：将请求转换为目标格式
6. **后端调用**：转发到实际 API 端点
7. **响应转换**：将响应转换回源格式
8. **统计记录**：记录请求指标

### 适配器矩阵

| 源 → 目标 | OpenAI | Claude | Gemini |
|-----------|--------|--------|--------|
| **OpenAI** | 直通 | claude-to-openai | gemini-to-openai |
| **Claude** | openai-to-claude | 直通 | gemini-to-claude |
| **Gemini** | openai-to-gemini | claude-to-gemini | 直通 |

## 🔧 详细配置

### config.json

```json
{
  "host": "localhost",
  "port": 5642,
  "database_path": "routes.db",
  "local_api_key": "sk-local-default-key",
  "redirect_enabled": true,
  "redirect_keyword": "proxy_auto",
  "redirect_target_model": "gpt-4-turbo",
  "minimize_to_tray": true,
  "auto_start": false
}
```

### 路由配置

路由存储在 SQLite 数据库（`routes.db`）中，表结构如下：

| 字段 | 类型 | 说明 |
|------|------|------|
| `name` | TEXT | 显示名称 |
| `model` | TEXT | 模型标识符（用于路由） |
| `api_url` | TEXT | 后端 API 基础 URL |
| `api_key` | TEXT | API 认证密钥 |
| `group` | TEXT | 可选分组 |
| `format` | TEXT | API 格式：`openai`、`claude`、`gemini` |
| `enabled` | INTEGER | 1=启用，0=禁用 |

## 🛠️ 开发指南

### 环境要求

- Go 1.22+
- Node.js 18+

### 开发模式

```bash
# 安装前端依赖
cd frontend && npm install && cd ..

# 直接使用 Go 运行
go run .
```

### 构建

```bash
# 为当前平台构建
go build -o anyproxyai .

# 或使用构建脚本
./build.sh        # Linux/macOS
build.bat         # Windows
```

### 项目结构

```
anyproxyai/
├── main.go                    # 应用入口，Wails 绑定
├── wails.json                 # Wails 配置
├── config.json                # 运行时配置
│
├── internal/                  # Go 后端模块
│   ├── adapters/              # API 格式转换器
│   │   ├── adapter.go         # 适配器接口
│   │   ├── anthropic.go       # Claude 适配器
│   │   ├── gemini.go          # Gemini 适配器
│   │   ├── openai_to_claude.go
│   │   ├── claude_to_openai.go
│   │   └── ...
│   ├── config/                # 配置管理
│   ├── database/              # SQLite 数据库
│   ├── router/                # HTTP 路由器 (Gin)
│   ├── service/               # 业务逻辑
│   │   ├── proxy_service.go   # 代理和流式处理
│   │   └── route_service.go   # 路由管理
│   └── system/                # 系统托盘、开机自启
│
└── frontend/                  # Vue 3 前端
    ├── src/
    │   ├── App.vue            # 主应用
    │   ├── i18n/              # 国际化
    │   └── components/        # UI 组件
    └── wailsjs/               # Wails 绑定
```

## 🔨 GitHub Actions 自动构建

本项目使用 GitHub Actions 进行自动化多平台构建。

### 触发构建

构建触发方式如下：

| 触发方式 | 构建 | 发布 | 示例 |
|---------|------|------|------|
| `package(...)` 提交 | ✅ | ❌ | `git commit -m "package(build): 修复问题"` |
| `tag(vX.X.X): 说明` 提交 | ✅ | ✅ | `git commit -m "tag(v1.0.0): 首次发布"` |
| 推送 `v*` 标签 | ✅ | ✅ | `git tag v1.0.0 && git push origin v1.0.0` |
| Pull Request | ✅ | ❌ | PR 到 main/master |
| 手动触发 | ✅ | ❌ | workflow_dispatch |

### 快速发布（推荐）

使用 `tag(版本号): 说明` 格式的提交信息，自动完成构建、创建标签、发布 Release：

```bash
# 这将会：构建所有平台 → 创建 v1.0.0 标签 → 发布 Release（说明自动填充）
git commit -m "tag(v1.0.0): 首次发布，支持多格式 API 转换"
git push origin main
```

Release 的说明会自动使用冒号后面的文字。

### 仅构建（不发布）

使用 `package(...)` 前缀进行仅构建的提交：

```bash
git commit -m "package(build): 修复 linux arm64 构建"
git push origin main
```

### 下载构建产物

1. 进入 GitHub 仓库的 **Actions** 标签页
2. 点击已完成的工作流运行
3. 下载各个架构的独立产物：
   - `anyproxyai-windows-amd64`
   - `anyproxyai-windows-arm64`
   - `anyproxyai-linux-amd64`
   - `anyproxyai-linux-arm64`
   - `anyproxyai-darwin-amd64`
   - `anyproxyai-darwin-arm64`

## ❓ 常见问题

### 安装问题

**Q: Windows 显示安全警告怎么办？**
A: 点击「更多信息」→「仍要运行」。应用未进行代码签名。

**Q: macOS 显示「无法打开」错误怎么办？**
A: 右键点击 → 打开 → 打开。或在「系统偏好设置」→「安全性与隐私」中允许。

**Q: 端口 5642 已被占用怎么办？**
A: 编辑 `config.json` 并修改 `port` 值，或在设置页面中修改。

### 使用问题

**Q: 格式转换是如何工作的？**
A: 代理从 URL 路径检测传入请求的格式，并根据路由的 `format` 设置将其转换为目标格式。

**Q: `proxy_auto` 是什么？**
A: 一个特殊关键字，重定向到你配置的目标模型，允许你在不同应用程序中使用单一模型名称。

**Q: Token 统计准确吗？**
A: Token 数量是基于响应数据的估算值。实际计费可能有所不同。

**Q: 如何切换语言？**
A: 点击右上角的语言图标，弹出语言切换窗口。设置会持久化保存。

### 开发问题

**Q: 如何添加新的适配器？**
A: 在 `internal/adapters/` 中实现 `Adapter` 接口，并在 `adapter.go` 中注册。

## � 参考证项目

本项目受以下项目启发并参考：

- [ccNexus](https://github.com/lich0821/ccNexus) - Claude Code Nexus
- [LLM-API-Transform-Proxy](https://github.com/wcpsoft/LLM-API-Transform-Proxy) - LLM API 转换代理

## 📄 许可证

本项目采用 [MIT 许可证](LICENSE) 开源。

---

<div align="center">
Made with ❤️ and Claude AI From China
</div>
